# Trading Dashboard — Project Constitution

Self-hosted multi-broker, multi-account trading dashboard. Stocks, forex, commodities, indexes, bonds, ETFs, futures, crypto, CFD, options, derivatives. FE+BE on IONOS VPS behind Cloudflare; broker gateways + PG-18 on NUC15PRO over WireGuard; heavy AI box (RTX 4080 + 64 GB) for large Ollama models on demand.

## Stack

- **BE:** Python 3.14 · FastAPI · SQLAlchemy 2.0 async · Alembic · Pydantic v2 · asyncpg
- **FE:** React 19 · Vite 7 · TS 6.0 strict · Tailwind v4 (`@theme`) · shadcn/ui (in-repo) · Zustand · Storybook 10 · klinecharts (Phase 9+)
- **Test:** Vitest 4 + RTL 16 (FE); pytest 9 + pytest-asyncio + httpx (BE); Playwright (Phase 5+)
- **Infra:** Redis 7 · PostgreSQL 18 native on Windows NUC (never containerized) · Docker Compose (docker-ce in WSL on NUC) · Cloudflare Tunnel (TLS at edge) + nginx defense-in-depth · CF Access + Google IdP
- **Broker SDKs:** `ib_async` · `futu-api` · `schwabdev` (read-only) · `alpaca-py`
- **AI:** Ollama — 7-8B on NUC, 14-70B on heavy box (WoL)
- **Pkg:** pnpm (FE) + uv (BE); Node 24 LTS via Corepack
- **Lint:** ruff + mypy --strict (BE); ESLint 9 flat + `eslint-plugin-boundaries` + `jsx-a11y` + Stylelint (FE); pre-commit + commitlint

Versioning: latest stable at scaffold time; pin via lockfiles only.

### FE invariants
- **Fonts:** Noto only, self-hosted `.woff2` in `frontend/public/fonts/` (3 Latin + 5 CJK). `unicode-range` + `lang`-driven variants. `langForMarket(exchange)` from `src/services/lang.ts`.
- **Mobile-first:** bottom tab bar (mobile) / sidebar (desktop). Min 2.75 rem touch targets. Tables collapse to cards below `md`.
- **CSS units:** rem/%/vh/vw only — no `px`, no `em`. Stylelint `unit-disallowed-list` enforces.

### Component layers (one-way deps, enforced by `eslint-plugin-boundaries`)

| Layer | Path | May import from |
|---|---|---|
| `design-tokens/` | `src/design-tokens/**` | — (leaf; only place rem/hex/font literals live) |
| `components/primitives/` | `src/components/primitives/**` | tokens, lib |
| `components/patterns/` | `src/components/patterns/**` | tokens, primitives, patterns, lib |
| `components/layout/` | `src/components/layout/**` | tokens, primitives, patterns, layout, features, lib |
| `features/` | `src/features/**` | everything |

Primitives + patterns ship `.stories.tsx` + `.test.tsx`. Features tested E2E. Boundaries config in `frontend/eslint.config.mjs` is source of truth.

### FE runtime (Phase 3+)
- **Routing:** TanStack Router file-based under `frontend/src/routes/`. `routeTree.gen.ts` is gitignored, regenerated by `pnpm tsr generate` (auto via typecheck/test). Don't hand-edit.
- **Scoped state:** features use `useActiveStores()`. Never import `@/stores/scoped/*` directly (no-restricted-imports).
- **Service ticker:** `getServices()` lazy. Tests/Storybook call `setTickingEnabled(false)`.
- **Mode toggle:** paper→live needs confirm dialog; live→paper direct. Cold load = paper. Mode on `<body data-mode>`.
- **Vite dev proxy:** `/api` + `/health` → `http://10.10.0.2:8000` (NUC over WG).

## Broker adapters (Phase 4+)

Per-broker invariants live in phase-memory files (read before changing those surfaces): `phase4_sidecar_topology.md` (IBKR mTLS+CRL+watchdog), `phase5a_shipped.md` (NLV cache), `phase5b_shipped.md` (trade execution), `phase5c_shipped.md` (modify+bracket+fills), `phase6_futu_topology.md`, `phase7a_schwab_topology.md` (OAuth+two-tier+BackendCallback), `phase7b1_shipped.md` (quote engine+WS gateway), `phase7c_alpaca_topology.md` (in-cluster docker+30-symbol cap), `phase9_shipped.md` (charting v1: bar aggregator + historical store + 45 indicators + drag-modify SL/TP), `phase10a_shipped.md` (risk gate at station 4 — 7 checks + admin CRUD + FE WARN/BLOCK banners across preview/place_order/modify_order), `phase10b1_shipped.md` (position-sizing v0.10.2: 3 methods + VolatilityService singleton + /trade/sizing page + bars_1d CAGG), `phase10b2_shipped.md` (portfolio rollup v0.10.3: account_balance_snapshots hypertable + CAGGs + BalanceSnapshotWriter + 3 REST endpoints + WS gateway + /portfolio/rollup page), `phase11a_shipped.md` (AI router foundation v0.11.0.8: LiteLLM proxy + 8 capabilities + Redis-backed master-key auth callback + 4 REST `/api/ai/*` + 2 WS `/ws/ai/*` + heavy-box WoL + orphan-recovery sweeper + FE `/ai/chat` + `/admin/ai` + TradeTicketAiSection). Don't copy that detail here.

Cross-cutting load-bearing rules:
- **Boundary stripping:** `AccountResponse` to FE = `id, broker_id, alias, mode, currency_base, display_order` only. Never `gateway_label`/`account_number`. `account_id` UUID is the only FE handle; `AccountService._resolve_account` is the single chokepoint.
- **Race-free soft-delete:** `BrokerDiscoverer` only soft-deletes rows whose `last_seen_via = ANY(:healthy_labels)` THIS tick. All-unhealthy → empty predicate → zero deletions.
- **Maintenance:** `app/services/ibkr_maintenance.py` is source of truth. Backend returns `503 + Retry-After` during reset; watchdog skips weekend reset.
- **NUC ops:** `deploy/nuc/` (PowerShell launchers + watchdog + Pester). `provision-and-publish.ps1` rotates mTLS; `revoke-cert.ps1` revokes; `renew-sidecar-mtls.ps1` rolls one at a time.
- **Risk gate (Phase 10a, shipped v0.10.0):** validation pipeline has 5 stations including the risk gate. `app/services/risk_service.py::RiskService.evaluate(ctx, mode)` is the chokepoint, called inline in `orders_service.preview_order`, `orders_service.place_order`, and `orders_service.modify_order` (D2 file-split skipped — gate works inline). 7 checks: kill switches, max-daily-loss, PDT (Redis in-flight counter), cross-broker concentration, BP buffer, sidecar margin preview (asymmetric preview/place_order fail policy). Audit rows in `risk_decisions` on BLOCK (fail-OPEN per spec §4 — `risk_audit_insert_failures_total` Counter). `side` is lowercased at the audit boundary to satisfy the CHECK constraint. Admin surface: `/api/risk/limits` + `/api/risk/decisions` (read, JWT) and `/api/admin/risk-limits` (POST/PUT/DELETE soft-delete) + `/api/admin/accounts/{id}/kill-switch` (UPSERT) with CSRF nonce; cap-edit invalidation via `app_config:invalidate:risk_limits` and `app_config:invalidate:kill_switch` Redis pubsub. FE: `TradeTicketModal` renders WARN (acknowledge gate) + BLOCK rows from `PreviewResponse.risk_warnings`/`risk_blockers` and from 422 `risk_gate_blocked` (`RiskGateBlockedError`). Pages: `/admin/risk` (limits CRUD), `/admin/risk/decisions` (feed), `/admin/accounts` (kill-switch row). In-flight Redis counters require single-replica today; multi-worker locking deferred to Phase 24. Gate gated on `isinstance(db, AsyncSession)` so existing stub-Session tests stay green.
- **Position sizing (Phase 10b.1, shipped v0.10.2):** 3 sizing methods (fixed-fractional, fixed-risk-per-trade, vol-targeted) producing a suggested qty + pre-validated risk-gate verdict. `app/services/position_sizing_service.py::PositionSizingService` is the per-request orchestrator; `app/services/volatility_service.py::VolatilityService` is a lifespan singleton at `app.state.vol_service` that reads `bars_1d` (TimescaleDB continuous aggregate over `bars_1m`, alembic 0038) and Redis-caches realized-vol + ATR. Vol formula is `stddev(log returns) × sqrt(252)` over 14 bars, NOT ATR (architect C2). Endpoints: `POST /api/risk/position-size` (JWT, rate-limited 20/s burst via in-process deque limiter per `(jwt_subject, account_id)`), `GET /api/risk/sizing-defaults/{id}` (JWT), `PUT /api/admin/sizing-defaults/{id}` (JWT admin + CSRF nonce). Sizing defaults persist in `app_config` namespace `risk_sizing`. The sizer's gate-evaluation has no side-effects (PDT mint + audit live in `orders_service` AFTER the gate, not inside `evaluate()`), so the spec's `dry_run` flag was unnecessary. `SizingRequest` accepts EITHER `instrument_id` (BIGINT, from /trade/sizing page) OR `(conid + broker_id)` (from TradeTicketModal which has no instrument_id at hand); server resolves via `InstrumentResolver.find_by_alias`. FE surfaces: collapsible sizing section in `TradeTicketModal` (between TIF and Preview; "Use this size" overwrites qty; sizing-scoped WARN+BLOCK banners with `aria-label="Risk gate {warnings,blockers} (sizing)"`), standalone page at `/trade/sizing` (3-column comparison, URL-persisted search params). 6 Prometheus metrics: `position_sizing_compute_total{method,account_currency,verdict}`, `position_sizing_latency_seconds{method}`, `volatility_cache_{hits,misses}_total`, `position_sizing_vol_unavailable_total`, `position_sizing_admin_writes_total{field}`. Kelly criterion deferred to Phase 19 (depends on strategy-tagged backtest stats).
- **Multi-account portfolio rollup (Phase 10b.2, shipped v0.10.3):** Cross-broker NLV + intraday/30d/1y curve + exposure-by-asset-class + per-instrument drill at `/portfolio/rollup`. Hypertable `account_balance_snapshots` (alembic 0039) + 1h/1d CAGGs (alembic 0040, `materialized_only=false` for real-time aggregation; sync backfill via `op.get_context().autocommit_block()` because `CALL refresh_continuous_aggregate` is a PROCEDURE that COMMITs internally). `app/services/balance_snapshot_writer.py::BalanceSnapshotWriter` is wired into `brokers.py:1449` NLV UPDATE savepoint with `RETURNING id` and a per-tick `_pending_publish_account_ids` buffer; SQL uses `clock_timestamp()` not `now()` so multi-account inserts in the same outer TX get distinct `ts` (review HIGH #6 — ON CONFLICT was silently collapsing them). `app/services/portfolio_rollup_service.py::PortfolioRollupService` provides `compute_live` (per-account FX fault isolation — partial 200 not whole-rollup 503), `compute_curve` over 3 windows (per-currency FX cache; curve is informational and FX-failures degrade silently), `drill_asset_class` (per-instrument exposure with long_native + short_native CASE; global cap from `risk_limits`). Cost-basis exposure approximation (architect CRIT #2 — `positions.market_value_base` doesn't exist). `PortfolioRateLimiter` (10/s burst per `jwt_subject`, NOT per-account like position-sizing — portfolio is cross-account so all 3 endpoints + WS share the bucket) at `app/services/portfolio_rate_limiter.py`; `evict_stale` called after every successful check. Endpoints: `GET /api/portfolio/{rollup,curve,drill}` (JWT) + `WS /ws/portfolio/rollup` (CSWSH origin check pre-accept, pubsub.listen() not get_message polling, 250ms compute cache + 500ms debounce, 2s `asyncio.wait_for` send timeout, heartbeat 30s emits `{type:"stale",account_ids:[...]}`, v=1 frame schema, connection cap 20, recv-drain task for `WebSocketDisconnect` detection, bounded backoff reconnect on FE 500ms/1.5s/5s/15s). `base` validated against `SUPPORTED_BASE={GBP,USD,EUR,HKD,JPY,AUD}` frozenset. 9 Prometheus metrics under `portfolio_rollup_*`. FE: `services/portfolio/` (TanStack-Query hybrid hook with WS push via setQueryData + 10s REST poll fallback) + zustand-persist `stores/global/portfolio.ts` (migrate guard against non-string persisted values) + `/portfolio/rollup` route + 5 components (KPI bar with Live/Polling badge + partial badge + base select, SVG sparkline curve chart with window toggle, per-account table, exposure list, drill drawer with `aria-modal` + Escape close + verdict-coloured rows). Known limitations documented in CHANGELOG: FE drops `stale` frames, no end-to-end brokers→writer→WS test, `portfolio_rollup_ws_publish_total` is overloaded across Redis-publish + WS-send (split next phase). Single-replica today; multi-worker deferred to Phase 24.
- **AI router foundation (Phase 11a, shipped v0.11.0.8):** 8-capability multi-provider AI router (`LOCAL_ONLY`, `LONG_CONTEXT`, `REALTIME_SENTIMENT`, `STRUCTURED_OUTPUT`, `BULK_CHEAP`, `REASONING`, `NUMERICAL`, `CODING`) routed through a LiteLLM proxy with Redis-backed master-key auth callback. `app/services/ai/` core: `router.py::LiteLLMClient` (implements `AICompletionClient` ABC with `complete`, `stream`, `batch_complete`, `submit_job`, `get_job`, `cancel_job`); `rate_limiter.py::AIRouterRateLimiter` (sliding window 30/60s per `jwt_subject` + per-capability semaphores `{STRUCTURED_OUTPUT: 4, LOCAL_ONLY: 6, REASONING: 2, default: 8}`); `cost_ledger.py::CostLedger` (fire-and-forget bounded asyncio.Queue, batch insert into `ai_completions` hypertable from alembic 0041 every 1s/100 rows, fail-OPEN); `jobs.py::AIJobStore` (postgres-backed; alembic 0042 with `warming_started_at` + `inferring_started_at`); `orphan_sweeper.py` (30s loop in lifespan; 90s warming cutoff + 10min inferring cutoff; `ai_jobs_orphan_recovered_total{phase}` + `ai_jobs_orphan_sweep_failures_total`); `wol.py::HeavyBoxWoL` (3-failure circuit breaker for the heavy-box wake path); `capabilities.py::resolve_models` (router-level fallback iteration so cost ledger sees every attempt; LOCAL_ONLY removes cloud entries; `app_config[ai_router/capability_map]` is the source of truth). `secrets.py` reads `app_secrets[ai_provider/*]` with 60s TTL cache + pubsub invalidation. `litellm_auth_callback.py` is Redis-backed (cluster-safe). REST endpoints (`app/api/ai.py`): `POST /api/ai/complete` (sync warm-route, returns CompletionResult with `fallback_chain`), `POST /api/ai/jobs` (202 + `{job_id}` for cold-start), `GET /api/ai/jobs/{id}` + `DELETE /api/ai/jobs/{id}` (**404 not 403 on both unknown-id AND cross-jwt-subject** — existence-oracle defence). WS endpoints (`app/api/ws_ai.py`): `WS /ws/ai/chat` (per-connection 1 active stream + 5 turns/min sliding window, 10s send timeout chat-streaming override), `WS /ws/ai/jobs/{id}` (pubsub channel `ai:job:{id}`, **allowlisted extras** `{error_code, model, response, fallback_chain}` — unknown keys dropped to prevent silent leakage). All endpoints use `_guarded_ai_call` helper (LOCAL_ONLY API-boundary check + tool-calling 501 + rate-limit 429 + 5-arm exception mapping). LOCAL_ONLY enforcement is 3-layer defence-in-depth: API boundary, `resolve_models()`, LiteLLM auth callback. FE surfaces: `/ai/chat` route (`features/ai/ChatPage.tsx` + `useChatStream` hook with bounded backoff `[500,1500,5000,15000]` reconnect + `mountedRef` + per-conn turn-rate UI feedback + same-origin WS URL guard), `/admin/ai` route (capability map JSON editor + provider key CRUD with CSRF nonce via `services/admin/api.ts::mintCsrfNonce`; cost-ledger view + heavy-box badge are 11b placeholders), `TradeTicketAiSection` inserted into `TradeTicketModal.tsx` ABOVE sizing (one-shot `STRUCTURED_OUTPUT` via `useTradeContext` with **graceful-degrade**: never throws; renders "AI context unavailable" instead of blocking the ticket; request payload is symbol+side+qty ONLY — no account/NLV/positions). `stores/global/ai.ts` zustand-persist for chat history (capped 200, migrate guard against corrupted localStorage). Alerts engine (11b) + Telegram (11c, 11d) shipped under this umbrella.
- **Options single-leg (Phase 12, shipped v0.12.1):** Option chain viewer at `/options/chain` (butterfly layout, expiry tabs, 2 Hz WS push). `OptionDetailsSection` injected into `TradeTicketModal` for OPTION asset class. Exercise elections at `/options/events` (idempotent, CSRF header-only). Core services: `OptionChainService` (currency-keyed source routing, Redis singleflight, market-aware TTL), `OptionGreeksService` (upsert guard, evict_stale), `ExerciseService` (idempotency + 5/min rate limit). Options risk gate in `risk_service._check_options_exposure`: trading-level gate, naked-short L1/L2/L3, cash-secured-put reserve check at L2 (BLOCK), expiry cutoff BLOCK, 0DTE WARN, assignment-risk WARN (5-day window + delta≥0.7); single `_get_option_meta` helper (1 DB round-trip); fail-open when exchange is None. `orders_service._native_notional` multiplied by `ctx.multiplier` on all 3 branches; `multiplier` + `position_effect` resolved from `instruments.meta`. 11 Prometheus metrics. 4 broker proto RPCs (`GetOptionChain`, `GetOptionExpirations`, `StreamOptionGreeks`, `ExerciseOption`). Alembic 0047: `OPTION` enum, `position_effect`/`tax_treatment` columns (named CHECK constraints), `option_greeks` hypertable, `exercise_elections` table. Deferred: Schwab execution (upstream 401), Greeks in risk gate, TicksSubscriber wiring, Monaco swap.
- **Telegram trade execution (Phase 11d, shipped v0.11.3.0):** `/place_order` two-step flow (preview → `/confirm`) wired into existing aiogram bot. `app/services/telegram/order_flow.py` owns the full state machine: `parse_place_order` (symbol/side/qty parser with `_DECIMAL_8_RE` price validation, HTML injection guard), `resolve_instrument` (instruments table lookup → live broker `search_contracts` fallback → preferred-exchange filter → ambiguity guard → cache insert), `handle_place_order` (account query, single-account fast-path, multi-account disambiguation via `telegram:order:acct_select:{chat_id}:{from_user_id}` Redis key EX 120s), `handle_account_selection` (numeric reply consumer, returns bool consumed), `handle_confirm` (GETDEL `telegram:order:pending:{chat_id}:{from_user_id}` — real single-use gate; mints 30s web nonce `nonce:order:{account_id}:{nonce_uuid}` with `{payload_hash, rth_at_mint}` envelope via `orders_service._preview_payload_hash` + `_is_regular_trading_hours`; calls `place_order` unchanged; `client_order_id = f"telegram-{uuid4()}"`; live accounts require `/confirm LIVE`), `handle_cancel_order` (DEL both keys, read-bucket rate limit). `check_trade` bucket added to `TelegramRateLimiter` (5/min, **fail-CLOSED** on Redis error — only money-moving bucket). `commands.py` extended with `registry`, `capability`, `cfg` optional kwargs (backward-compatible); numeric account-selection handler registered before AI catch-all. Risk gate, PDT counters, and broker dispatch run unconditionally — Telegram is not a bypass path. 6 Prometheus metrics: `telegram_order_{attempts,previews,confirms,cancels}_total`, `telegram_rate_limiter_trade_block_total`, `telegram_order_e2e_seconds`. 63 telegram tests; 970 total BE tests.

## Configuration & topology

Runtime settings in DB (`app_config` + `app_secrets` Fernet-encrypted), not `.env`. **Don't add new `.env` keys beyond bootstrap.** Edit at runtime via `POST /api/admin/config`/`/secrets`. See [`docs/CONFIG.md`](docs/CONFIG.md).

WG topology + Postgres connectivity + Windows-side paths: [`docs/NETWORK.md`](docs/NETWORK.md). Key invariants: NUC = dev host (WSL2 at `/home/joseph/dashboard`); PG at `10.10.0.2:5432` over WG; VPS prod at `/home/trader/trading-dashboard`; SSH via `ssh -p 2222 trader@88.208.197.219`. Directory layout: `docs/superpowers/specs/2026-04-21-phase0-scaffold-design.md §3`.

## Coding conventions

Full rules in [`docs/CONVENTIONS.md`](docs/CONVENTIONS.md). Load-bearing one-liners:
- **Python:** 3.14; type hints everywhere; async-only; structlog (no `print`); no bare `except:`; `except (A, B) as exc:` (Py3 tuple-catch).
- **TS:** strict; no `any` without disable + reason; function components only.
- **SQL:** schema via Alembic only; `snake_case`; `TIMESTAMPTZ`; money in `NUMERIC(20, 8)`.
- **Git:** conventional commits; body ≤100 chars; squash-merge; never commit `.env`/`*.key`/`secrets/*`.

Common dev/lint/migrate/deploy commands: [`docs/COMMANDS.md`](docs/COMMANDS.md). Prod deploy: `./scripts/deploy.sh` (rsync + remote build + nginx reload).

## Security

- Never log API keys/tokens/passwords — structlog redacts via processor in `app/core/logging.py`.
- Broker creds in `app_secrets` (Fernet) from Phase 2. Never in git.
- Postgres reachable only via WG (VPS) and LAN/WSL (NUC). Never public.
- FE never sees broker creds.
- Trade-execution endpoints require confirmation nonce (CSRF).

## Roadmap & goals

Phase 7 → v1.0.0 locked in [`docs/ROADMAP.md`](docs/ROADMAP.md). End-state: every IBKR/Futu/Schwab/Alpaca asset class + relevant order types, multi-source streaming quotes, charting, AI alerts/scanner, autonomous self-refining bots (param-tuning + LLM-in-loop ceiling — no raw RL), UK CGT (S104 + SA108), PWA mobile.

**Non-goals:** native RN app (PWA covers); raw RL bots (overfit); paper sim (use broker paper); multi-tenant.

## Phase workflow

Every phase: brainstorm → spec self-review → **architect review (apply CRIT+HIGH+MED inline)** → user approval → plan → impl (subagent-driven) → close-out (CLAUDE.md/CHANGELOG.md/TASKS.md, tag, push). Per-commit reviewer chain (spec-compliance + code-quality + lang-reviewer minimum + others when triggered). Full workflow + tooling list + reviewer table: [`docs/PHASE-WORKFLOW.md`](docs/PHASE-WORKFLOW.md). Catalog: memory `project_tooling_inventory.md`.

### Subagent model routing (updated 2026-05-14, Phase 12)

**Coding is split between Codex and local Qwen.** Anthropic subagents do **not** write production code — they review.

**By task character (primary decision axis):**

| Task character | Route to | Why |
|---|---|---|
| Multi-file refactors (≥3 files, cross-cutting renames, file splits) | **Codex** | Maintains coherent context across many files |
| Lua scripts / Redis atomics / narrow vendor-API specifics | **Codex** | Broader training-set coverage of API edges |
| Long-context analysis (full spec + plan + repo) | **Codex** | 256K context utilized fully |
| Self-contained module writes (new file, single class, well-specified) | **Qwen** (local) | Greenfield from focused prompts — 40 t/s, completes fully |
| TDD test writers (tests matching a known spec) | **Qwen** | Mechanical, structured output |
| Schema-driven SQL / Alembic migrations | **Qwen** | Highly structured form |
| Protobuf / gRPC schema additions (new messages + RPCs, single .proto file) | **Qwen** | Highly structured, spec-driven; thinking helps with field numbering |
| Pydantic v2 schema / discriminated union writes | **Qwen** | Structured, type-checked form; thinking prevents discriminator mistakes |
| Single new risk check (well-specified, isolated method in risk_service.py) | **Qwen** | Bounded scope; thinking handles the sub-check logic |
| Prometheus metric wiring (labels + counter/gauge/histogram in known file) | **Qwen** | Mechanical from spec; thinking catches label mismatches |
| Existing-code integration / multi-site judgment across ≥2 files | **Opus direct** | Holistic pattern matching; preserves orchestration context |

**Qwen thinking usage:** Always prepend `<think>\n</think>` to the prompt at the raw completions endpoint to suppress runaway thinking. Thinking is useful for: protobuf field numbering, discriminator logic, sub-check edge cases. Budget: allow up to 512 thinking tokens; truncate if >1024 (signals runaway).

**By availability (fallback ladder when the primary is unavailable):**

| Priority | Provider | Endpoint | Trigger |
|---|---|---|---|
| 1 | Codex via `codex exec` | OpenAI API | Default for "Codex tasks" above |
| 2 | **Local Qwen3.6-35B-A3B Q4_K_M** via llama.cpp | `http://192.168.50.30:11435/v1/completions` (~40 t/s decode, 140 t/s prefill) | Default for "Qwen tasks"; or Codex rate-limit |
| 3 | Qwen3-Coder-Next Q3_K_XL 79B via llama.cpp | same endpoint | 35B-A3B produces 2+ unusable outputs in a row |
| 4 | Qwen2.5-Coder-14B (LKG) | Ollama `:11434`, `qwen2.5-coder:14b` | All Qwen3.x fail |
| 5 | Opus main thread takes the task | — | Both Codex AND Qwen ladder exhausted |

**Reviewer dispatches (unchanged):**

| Reviewer | Model |
|---|---|
| spec-compliance / `python-reviewer` / `typescript-reviewer` | `haiku` |
| `code-reviewer` / `security-reviewer` / `database-reviewer` / `silent-failure-hunter` | `sonnet` |
| `ARCHITECT-REVIEW` (once-per-phase, skill) | `opus` |

Pass `model: "haiku"`/`"sonnet"` to the `Agent` tool for per-call override.

**User overrides** ("use codex", "use qwen", "claude take over") honor the named model. Memory: `feedback_codex_fallback.md`, `feedback_qwen_protocol.md`, `feedback_qwen_model_preference.md`.

## When Claude makes changes

- Tests after edits: `docker compose exec backend pytest` + `cd frontend && pnpm test`.
- Regenerate types when API schemas change: `scripts/gen-types.sh`.
- Never modify `brokers/base.py` without updating every concrete adapter.
- Schema changes → Alembic migration, not raw model edits.
- Prefer editing existing files. Use `/frontend-design` skill for FE design.
- GitHub is canonical repo. Update CLAUDE.md/CHANGELOG.md/TASKS.md every phase close; commit.
